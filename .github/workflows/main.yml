
on:
  push:
     branches:
         - master 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:   
      - name: 'testing gitgub actions'
        run: pwd

      - name: 'testing printing'
        run: echo youssef
